apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: proverjay
servers: 1
agents: 1
ports:
  - port: 8087:80
    nodeFilters:
      - loadbalancer
options:
  k3d:
    wait: true
    timeout: 60s
  k3s:
    extraArgs:
      - arg: --disable=traefik
        nodeFilters:
          - server:*
